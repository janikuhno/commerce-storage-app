import React, { Fragment } from 'react';

const EditProduct = ({ product, allProducts, setAllProducts }) => {
  return (
    <Fragment>
      <div></div>
    </Fragment>
  );
};

export default EditProduct;
